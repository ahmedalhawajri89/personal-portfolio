# dependencies
node_modules/

# build output
.next/
out/

# secrets
.env*.local

# misc
.DS_Store
Thumbs.db

# agent instruction files — regenerated by `next dev` on every run
# (node_modules/next/dist/server/lib/generate-agent-files.js), so they are
# kept out of the repo rather than committed and re-dirtied
AGENTS.md
CLAUDE.md
